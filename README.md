# trippo-agent

Group travel planning agent for the Google Cloud Rapid Agent Hackathon (MongoDB track).

Built with Gemini 3 + Google Cloud Agent Builder + MongoDB MCP.

See `LOOP_BRIEF.md` for the build brief and `docs/` for the design (filled in by the build loop).

## Status

In-progress. Initial scaffold being generated by autonomous loop.
